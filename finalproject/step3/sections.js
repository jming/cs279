globals.green = '#0F9D58';
globals.red = '#DB4437';
globals.weight = 7;

// display sections of routes color-coded by accessibility
// section - Section object (see app.js)
function showRouteAccessibility(section) {
    getRoute(section);
}
